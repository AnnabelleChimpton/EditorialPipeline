const pipelineConfig = {
  /** Display name shown in the navbar and page title */
  pipelineName: "My Pipeline",
  /** Absolute path to your OpenClaw workspace (e.g. ~/.openclaw/workspace-myproject) */
  workspaceRoot: "",
};
export default pipelineConfig;
